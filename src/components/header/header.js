import './header.scss';

const Header = () => {
    return (
        <header className="header">
            <h1 className="header__title">Our Coffee</h1>
        </header>
    );
}

export default Header;